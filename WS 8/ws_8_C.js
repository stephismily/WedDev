function runCode(){
    //Try to select the same element (the <p id="para1">) using: getElementById
    let paId = document.getElementById("para1");

    //Try to select the same element (the <p id="para1">) using: querySelector("#para1")
    let paQ = document.querySelector("#para1");
    
    //getElementsByClassName("intro")
 getElementsByClassName("intro")
    let intros = document.getElementsByClassName("intro");

    //getElementsByClassName("intro"): querySelectorAll(".intro")
    let ints = document.querySelectorAll(".intro");
}